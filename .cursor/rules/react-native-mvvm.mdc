---
description: MVVM architecture for React Native screens — hooks as view models
globs: screens/**/*.tsx
alwaysApply: false
---

# React Native MVVM

Prefer MVVM for screens. Screen components are the **View**; custom hooks are the **ViewModel**.

## View (screen component)
- Render UI only — layout, styling, and wiring props/handlers from the view model
- Avoid business logic, API calls, validation, and side effects in the screen file
- Keep navigation setup (`useLayoutEffect`, header options) minimal; move reusable logic into the view model hook when it grows

## ViewModel (custom hook)
- One hook per screen, named `use<ScreenName>ViewModel` (e.g. `useSignUpViewModel` for `screens/sign-up/`)
- Colocate in `screens/<name>/use<Name>ViewModel.ts` (or `.tsx` when the hook returns JSX for header options)
- Own state, derived data, mutations/queries, validation, and side effects
- Return a plain object: `{ state, actions, status }` (or similar) for the view to consume

## Model
- Data types live in `types/`
- API/data access lives in `api/` (React Query hooks, Supabase calls, etc.)
- ViewModels compose model-layer hooks; views do not call `api/` directly

## Route files
- Files in `app/` are thin re-exports only: `export { default } from '~/screens/<name>';`
- Layout files (`_layout.tsx`) stay in `app/`

## Example

```tsx
// screens/sign-up/useSignUpViewModel.ts
export function useSignUpViewModel() {
  const router = useRouter();
  const { mutate: signUp, isPending } = useSignUp();

  const signUpUser = (email: string, password: string) => { /* ... */ };
  const goToSignIn = () => router.back();

  return { status: { isPending }, actions: { signUpUser, goToSignIn } };
}

// screens/sign-up/index.tsx — View only
export default function SignUpScreen() {
  const { status, actions } = useSignUpViewModel();
  // render only
}

// app/(auth)/sign-up.tsx — Route re-export
export { default } from '~/screens/sign-up';
```

## Existing patterns to follow
- `useFilteredPlaces` is a good composable hook — view models compose hooks like this; views do not import them directly
- Redirect-only screens (e.g. `screens/home/`) may omit a view model when logic is trivial
